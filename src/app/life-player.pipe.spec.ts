import { LifePlayerPipe } from './life-player.pipe';

describe('LifePlayerPipe', () => {
  it('create an instance', () => {
    const pipe = new LifePlayerPipe();
    expect(pipe).toBeTruthy();
  });
});
