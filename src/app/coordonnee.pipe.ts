import { Pipe, PipeTransform } from '@angular/core';

@Pipe({
  name: 'coordonnee'
})
export class CoordonneePipe implements PipeTransform {
const CoinHautGauche = 0;
const CoinBasDroite =  0;
const 

  transform(value: any, args?: any): any {
    return null;
  }

}
